## Fundamentos da Sintaxe
---

**Declaração de variáveis**
```rust

let x = 10;             // Variável imutável com inferência de tipo (i32)
let mut y = 20;         // Variável mutável (pode ser alterada)
y = y + 5;

// Tipos primitivos explícitos
let numero: i32 = 42;          // inteiro 32-bit
let decimal: f64 = 3.14;       // ponto flutuante 64-bit
let booleano: bool = true;     // booleano
let caractere: char = '🦀';    // caractere Unicode

// Strings em Rust
let texto_fixo: &str = "Olá";                  // Slice de string (imutável, armazenada na stack)
let texto_dinamico: String = String::from("Olá"); // String alocada dinamicamente (na heap)

// Diferença entre &str e String:
//
// - `&str` é uma fatia de string, geralmente literal, imutável e mais leve.
// - `String` é uma estrutura que permite modificar e gerenciar strings em tempo de execução.
//
// Exemplo:
let nome_estatico: &str = "Ana"; // Literal (&str)
let mut nome_dinamico = String::from("Ana");
nome_dinamico.push_str " Silva"; // Agora é "Ana Silva"

```

**Comentários**
```rust

// Comentário de linha

/*
Comentário de bloco
com várias linhas
*/
```

**Operadores**

```rust
let soma = 2 + 3;
let subtracao = 5 - 1;
let produto = 4 * 2;
let divisao = 8 / 2;
let resto = 10 % 3;

let comparacao = 5 > 3;           // true
let igualdade = 10 == 10;         // true
let logico = true && false;       // false

```
